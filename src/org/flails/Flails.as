package org.flails {
  
  public class Flails {
/*    public var request:FlailsRequest;
    
    public function Flails(resource:String) {
      this.request = new FlailsRequest(resource);
    }
    
    public function findAll():void {
      request.dispatch(resultHandler);
    }
    
    public function findByID(id:uint):void {
      request.resourceID = id;
      request.dispatch(resultHandler);
    }
    
    public function create(attributes:Object):Boolean {
      request.resourceID = attributes.id;
      request.method = request.POST;
      request.push_params(attributes);
      request.dispatch(resultHandler);
    }
    
    public function update(attributes:Object):Boolean {
      request.resourceID = attributes.id;
      request.method = request.PUT;
      request.push_params(attributes);
      request.dispatch(resultHandler);
    }
    
    public function destroy(id:unum):Boolean {
      request.resourceID = attributes.id;
      request.method = request.DELETE;
      request.dispatch(resultHandler);
    }*/
       
  }
}